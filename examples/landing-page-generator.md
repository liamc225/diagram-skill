# Landing Page Generator — Architecture

5-step pipeline that takes a job posting URL, researches the company, generates personalized content, and deploys a custom landing page to Vercel.

```mermaid
graph TD
    URL(("Job URL"))

    subgraph Pipeline["page-generator/pipeline"]
        Extract["extract.py<br/>Parse job posting"]
        Research["research.py<br/>Company intel + stack + logo"]
        Content["content.py<br/>Hero pitch, fit points, bridge"]
        Match["match.py<br/>Stack overlap vs profile"]
        Generate["generate.py<br/>Render HTML + deploy"]
    end

    subgraph APIs["External APIs"]
        Jina["Jina Reader"]
        Tavily["Tavily Search"]
        Firecrawl["Firecrawl"]
        OpenRouter["OpenRouter<br/>Gemini Flash"]
    end

    Profile[("profile.json")]
    Vercel["Vercel"]
    Page(("Live Page<br/>liamclarke.vercel.app"))

    URL --> Extract
    Extract -->|fetch + render| Jina
    Extract -->|structured extraction| OpenRouter
    Extract --> Research
    Research -->|company search| Tavily
    Research -->|scrape Crunchbase + logo| Firecrawl
    Research -->|extract intel| OpenRouter
    Research --> Content
    Content -->|generate copy| OpenRouter
    Content --> Match
    Match <-->|read| Profile
    Match --> Generate
    Generate -->|"vercel --prod"| Vercel
    Vercel --> Page

    style Pipeline fill:#1a1a2e,stroke:#6366f1,color:#e2e8f0
    style APIs fill:#1a1a2e,stroke:#f59e0b,color:#e2e8f0
    style URL fill:#22d3ee,stroke:#22d3ee,color:#0f172a
    style Page fill:#6366f1,stroke:#6366f1,color:#fff
    style Vercel fill:#0f172a,stroke:#22d3ee,color:#22d3ee
    style Profile fill:#0f172a,stroke:#22d3ee,color:#22d3ee
```

Generated by [diagram-skill](https://github.com/liamc225/diagram-skill)
