# Architecture Diagram — Next.js SaaS App

Auto-generated by `/diagram architecture` from a typical Next.js SaaS codebase with Prisma, Stripe, and NextAuth.

```mermaid
graph TB
    subgraph "Client"
        Browser["Next.js App Router<br/>(React Server Components)"]
    end

    subgraph "API Layer"
        Auth["NextAuth.js<br/>/api/auth/*"]
        API["API Routes<br/>/api/*"]
        Webhooks["Webhook Handlers<br/>/api/webhooks/*"]
    end

    subgraph "Services"
        UserSvc["User Service"]
        BillingSvc["Billing Service"]
        EmailSvc["Email Service"]
    end

    subgraph "Data"
        DB[(PostgreSQL)]
        Cache[(Redis)]
    end

    subgraph "External"
        Stripe["Stripe API"]
        Resend["Resend API"]
        OAuth["Google OAuth"]
    end

    Browser -->|"RSC / fetch"| API
    Browser -->|"session"| Auth
    Auth -->|"OAuth 2.0"| OAuth
    Auth -->|"Prisma"| DB

    API --> UserSvc
    API --> BillingSvc
    UserSvc -->|"Prisma"| DB
    UserSvc -->|"get/set"| Cache
    BillingSvc -->|"REST"| Stripe
    BillingSvc -->|"Prisma"| DB

    Stripe -->|"webhook POST"| Webhooks
    Webhooks --> BillingSvc
    Webhooks --> EmailSvc
    EmailSvc -->|"REST"| Resend
```

Generated by [diagram-skill](https://github.com/YOUR_USERNAME/diagram-skill)
